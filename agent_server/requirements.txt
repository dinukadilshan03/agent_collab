# Core web framework
fastapi==0.116.1
uvicorn==0.35.0

# Data validation (automatically installed with FastAPI)
pydantic==2.11.7

# Google Generative AI (newer SDK)
google-genai==1.31.0

# Environment variables
python-dotenv==1.1.1

# Core dependencies (automatically installed but pinned for stability)
requests==2.32.5
